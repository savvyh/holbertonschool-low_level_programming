#include "main.h"
/**
 * _islower - check lowercase character.
 * @c - character to check.
 * Return _islower : 1 if lowercase, else 0.
*/
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
